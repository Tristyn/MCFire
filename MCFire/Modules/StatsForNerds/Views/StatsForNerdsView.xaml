<UserControl x:Class="MCFire.Modules.StatsForNerds.Views.StatsForNerdsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:MCFire.Modules.StatsForNerds.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance viewModels:StatsForNerdsViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--RenderMode-->
        <DockPanel>
            <DockPanel VerticalAlignment="Top" DockPanel.Dock="Top">
                <CheckBox DockPanel.Dock="Right">Default</CheckBox>
                <ComboBox ItemsSource="{Binding RenderModes}" SelectedItem="{Binding SelectedRenderMode}" Text="Render Mode" />
            </DockPanel>
            <TextBlock Text="Woah" DockPanel.Dock="Top"></TextBlock>
        </DockPanel>
    </Grid>
</UserControl>
